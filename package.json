{
  "name": "posebird",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --sourcemap --outfile=public/main.js",
    "watch": "npm run build -- --watch",
    "dev-server": "node devserver.js",
    "dev": "run-p watch dev-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.12.14",
    "eslint": "^7.30.0",
    "express": "^4.17.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@natfaulk/supersimplelogger": "^1.0.3",
    "three": "^0.130.1"
  }
}
